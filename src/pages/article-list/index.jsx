import React from 'react';
import { Button } from 'antd';
import 'antd/dist/reset.css';

const ArticleListPage = () => (
    <div className="App">
      <Button  type="primary">article-list</Button>
    </div>
);

export default ArticleListPage;